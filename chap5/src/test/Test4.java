package test;
/*
1부터 100까지 임의의 수 10개를  배열에 저장하고, 정렬하여 출력하기
  1. 배열 선언, 생성.
  2. 임의의 수를 10개 배열에 저장
  3. 정렬
  4. 출력
*/
public class Test4 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		//1. 배열 선언 생성
		
		int num[] = new int[10];
		
		
		
		//3. 정렬
		
		//if(num[j] > num[j+1]) // 오름차순 - 앞에 숫자가 크면 바꿔,   	if(num[j] < num[j+1]) // 내림차순 - 뒤에 숫자가 작으면 바꿔
		
		/*
		 *	Arrays.sort(num); 		//num 배열을 정렬. 
		 *  Arrays.sort(num); 		//num 배열을 정렬.
		 * 
		 */
		
		
		
		
		// swap 알고리즘 -> 
		// boolean sorted = true;
		// sorted = false; 	//순서가 다 변경 된 경우
		// if(sorted) break; > 정렬이 다 됐으면 멈춰라. 
		
	}

}
